spring:
  ai:
    openai:
      api-key: ${COMPASS_API_KEY:}
      base-url: https://api.core42.ai
      chat:
        options:
          model: gpt-4.1
          temperature: 0.7

server:
  port: 8080
