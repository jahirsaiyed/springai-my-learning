spring:
  ai:
    mcp:
      server:
        name: ecommerce-mcp
        version: 1.0.0
        type: SYNC
    mcp-server:
      stdio: false

#  datasource:
#    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}?prepareThreshold=0
#    username: ${DB_USERNAME:postgres}
#    password: ${DB_PASSWORD:postgres}
#    driver-class-name: org.postgresql.Driver
  datasource:
    url: jdbc:postgresql://${DB_HOST:aws-1-ap-southeast-1.pooler.supabase.com}:${DB_PORT:6543}/${DB_NAME:postgres}?prepareThreshold=0
    username: ${DB_USERNAME:postgres.jubfkjoaymuuxnowiyvs}
    password: ${DB_PASSWORD:sgJB0BYqc1fHeMl4}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: ecommerce

server:
  port: ${MCP_SERVER_PORT:8081}
